Function returnEvenValues(Array){
 let evenNums = [];
  for(let i = 0; i < Array.length;
  if(Array[i] % 2 === 0) {
    evenNums.push(array[i];)
  }
}
  console.log(evenNums)

letArray = [1, 2, 4, 5, 7, 8];

returnEvenValues(Array);
